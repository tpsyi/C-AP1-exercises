#include <stdio.h>
#include <stdlib.h>

int main()
{
    /* Purge array from enemies */
//    int init_size = 10, new_size = 0;
//    float arr[init_size];
//    readFArr(init_size, arr);
//    printFArr(init_size, arr);
//    purge(arr, init_size, 0.0, &new_size); new_size = init_size - new_size;
//    printFArr(new_size, arr);
    /**/
//    search_arr();
}
